// function CurrentDate() {
//   const month = toLocaleString("en-US", { month: "long" });
//   const day = toLocaleString("en-US", { day: "2-digit" });
//   const year = getFullYear( year: "long");

//   console.log(`${month} ${day} ${year}`);
// }

// CurrentDate();

// export default CurrentDate;
